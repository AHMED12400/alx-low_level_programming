#include "main.h"
#include <stdio.h>

/**
 * main - check the code 
 *
 * Return: Always 0.
 */
void reset_to_98(int *n);

{
	int n;
	n = 98;

	*ptr = &n;
    n = 402;
    for (n = 402; n <= 402; reset_to_98(&n);
	putchar("n=%d\n", n);
	putchar("n=%d\n", n);
    return (0);
}
