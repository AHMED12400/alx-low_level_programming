#include "main.h"
#include <stdio.h>

/**
 * main - check the code 
 *
 * Return: Always 0.
 */
void reset_to_98(int *n)

{
	int n;
	n 402;
	int *ptr;
	putchar("n=%d\n", n);
	
	putchar("n=%d\n", reset_to_98(&n));

	return (0);
}
