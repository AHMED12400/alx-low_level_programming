#include "main.h"
#include <stdio.h>

/**
 * main - check the code 
 *
 * Return: Always 0.
 */
void reset_to_98(int *n);

{
	int n;
	*ptr = &n;
    n = 402;
    for (n = 402; n = 402; &n);
	putchar("n=%d\n", n);
    reset_to_98(&n);
	putchar("n=%d\n", n);
    return (0);
}
