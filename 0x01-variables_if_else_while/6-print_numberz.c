#include <stdio.h>

/**
 * main - print all single digits base on 10
 * Return: 0 and exit the program
 */

int main(void)
{
        int number;

        for (number = '0'; number <= '10'; number++)
        putchar(number);
        putchar('\n');

        return (0);
}i
