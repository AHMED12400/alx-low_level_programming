#include <stdio.h>

/**
 * main - print alphbet in lower case followed by new line
 * Return: 0 and exit the program
 */

int main(void)
	char letter

	for (letter = 'a'; letter <= 'z'; letter++)
	putchar(letter);
	putchar('\n');

	return (0);
